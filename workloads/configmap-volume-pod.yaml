apiVersion: v1
kind: Pod
metadata:
  name: configmap-volume-test
  namespace: dev
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      volumeMounts:
        - name: app-config
          mountPath: /etc/config
  volumes:
    - name: app-config
      configMap:
        name: app-config
